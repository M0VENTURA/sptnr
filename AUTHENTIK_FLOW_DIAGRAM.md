# Authentik Authentication Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER VISITS LOGIN PAGE                       │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         LOGIN PAGE (login.html)                      │
│                                                                       │
│  ╔═══════════════════════════════════════════════════════╗          │
│  ║                        Sptnr                          ║          │
│  ║               Music Rating & Library Manager          ║          │
│  ╚═══════════════════════════════════════════════════════╝          │
│                                                                       │
│  ┌─────────────────────────────────────────────────────┐            │
│  │ Username: [________________]                         │            │
│  │ Password: [________________]                         │            │
│  │                                                       │            │
│  │        [ Sign In with Navidrome ]  ◄── Traditional   │            │
│  └─────────────────────────────────────────────────────┘            │
│                                                                       │
│  if authentik_enabled:                                               │
│                                                                       │
│  ┌─────────────────────────────────────────────────────┐            │
│  │                     OR                               │            │
│  └─────────────────────────────────────────────────────┘            │
│                                                                       │
│  ┌─────────────────────────────────────────────────────┐            │
│  │    [ 🛡️  Sign in with Authentik ]  ◄── OAuth/SSO    │            │
│  └─────────────────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────────────────┘
                    │                           │
                    │                           │
      ┌─────────────┘                           └─────────────┐
      ▼                                                        ▼
┌──────────────────────────┐                    ┌──────────────────────────┐
│  NAVIDROME LOGIN         │                    │  AUTHENTIK OAUTH         │
│                          │                    │                          │
│  1. Verify credentials   │                    │  1. Redirect to Authentik│
│     with Navidrome API   │                    │     /login/authentik     │
│                          │                    │                          │
│  2. Create session       │                    │  2. User authenticates   │
│     - username           │                    │     on Authentik         │
│     - auth_method:       │                    │                          │
│       "navidrome"        │                    │  3. Callback received    │
│                          │                    │     /auth/callback       │
│  3. Redirect to dashboard│                    │                          │
└──────────────────────────┘                    │  4. Exchange code for    │
                                                │     access token         │
                                                │                          │
                                                │  5. Get user info        │
                                                │     - preferred_username │
                                                │     - email              │
                                                │     - sub (UUID)         │
                                                │                          │
                                                │  6. Create session       │
                                                │     - username           │
                                                │     - email              │
                                                │     - auth_method:       │
                                                │       "authentik"        │
                                                │     - user_info          │
                                                │                          │
                                                │  7. Redirect to dashboard│
                                                └──────────────────────────┘
                    │                                          │
                    └──────────────┬───────────────────────────┘
                                   ▼
                    ┌──────────────────────────────┐
                    │         DASHBOARD            │
                    │                              │
                    │   User is authenticated!     │
                    │   Can access all features    │
                    └──────────────────────────────┘
```

## Configuration Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                        config.yaml Structure                         │
└─────────────────────────────────────────────────────────────────────┘

authentik:
  enabled: true/false  ────────────┐
                                   │
  server_url: "https://..."  ──────┼─► Used in OAuth metadata URL
                                   │
  client_id: "..."  ───────────────┼─► OAuth client credentials
  client_secret: "..."  ───────────┘
                                   
  app_slug: "sptnr"  ──────────────┐
                                   │
                                   ├─► Builds metadata URL:
                                   │   {server_url}/application/o/{app_slug}/
                                   │   .well-known/openid-configuration
                                   │
  username_field:  ────────────────┘
    "preferred_username"  ─────────┐
      or "email"                   │
      or "sub"                     ├─► Determines which field from
                                   │   Authentik becomes the username
                                   │   in Sptnr session
                                   │
                                   ▼
                        session['username'] = user_info[username_field]
```

## Session Management

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Session Structure                            │
└─────────────────────────────────────────────────────────────────────┘

Navidrome Authentication:
  session = {
    'username': 'john_doe',
    'auth_method': 'navidrome',
    'permanent': True
  }

Authentik Authentication:
  session = {
    'username': 'john_doe',  # from configured username_field
    'email': 'john@example.com',
    'auth_method': 'authentik',
    'user_info': {
      'name': 'John Doe',
      'email': 'john@example.com',
      'sub': 'uuid-1234-5678-9abc'
    },
    'permanent': True
  }
```

## OAuth Security Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    OAuth 2.0 / OIDC Flow                            │
└─────────────────────────────────────────────────────────────────────┘

1. Authorization Request
   Sptnr → Authentik
   GET /application/o/authorize
     ?client_id={client_id}
     &redirect_uri=https://sptnr.example.com/auth/callback
     &response_type=code
     &scope=openid+email+profile

2. User Authentication
   User authenticates with Authentik
   (username/password, MFA, etc.)

3. Authorization Code
   Authentik → Sptnr
   GET /auth/callback?code={authorization_code}

4. Token Exchange
   Sptnr → Authentik
   POST /application/o/token/
     client_id={client_id}
     client_secret={client_secret}
     code={authorization_code}
     grant_type=authorization_code

5. Access Token Response
   Authentik → Sptnr
   {
     "access_token": "...",
     "token_type": "Bearer",
     "expires_in": 3600,
     "id_token": "..."
   }

6. UserInfo Request
   Sptnr → Authentik
   GET /application/o/userinfo/
     Authorization: Bearer {access_token}

7. UserInfo Response
   Authentik → Sptnr
   {
     "sub": "uuid",
     "preferred_username": "john_doe",
     "email": "john@example.com",
     "name": "John Doe",
     ...
   }

8. Session Created
   Sptnr creates authenticated session
   User is redirected to dashboard
```
