"""Playlist download-session repository — REMOVED.

The original implementation queried a ``playlist_download_sessions`` table
that was never created in the live schema; no code imports this module.
Kept as an inert stub so stray imports fail loudly instead of running SQL
against a non-existent table.
"""
